[project]
name = "newsflash-docs"
version = "0.1.0"
description = "Homepage and documentation website for the newsflash library"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "newsflash>=0.2.7",
]

[dependency-groups]
dev = [
    "ruff>=0.15.1",
]
